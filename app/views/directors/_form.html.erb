<%= form_with model: director do |f| %>
  <div>
    <%= f.label :name, "Nome" %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :birthdate, 'Data de nascimento' %>
    <%= f.date_field :birthdate %>
  </div>
  <div>
    <%= f.fields_for :country, director.country do |country| %>
      <%= country.label :name, 'País de origem' %>
      <%= country.text_field :name %>
    <% end %>
  </div>
  <div>
    <%= f.fields_for :genre, director.genre do |genre| %>
      <%= genre.label :name, 'Gênero favorito' %>
      <%= genre.text_field :name %>
    <% end %>
  </div>
  <%= f.submit "Enviar" %>
<% end %>